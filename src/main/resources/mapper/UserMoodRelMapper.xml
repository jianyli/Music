<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.li.dao.UserMoodRelDao">
    <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="com.li.entity.UserMoodRel">
        insert into user_mood_rel(user_id, mood_id) value (#{userId} ,#{moodId})
    </insert>
</mapper>